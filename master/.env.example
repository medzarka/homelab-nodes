# ==============================================================================
# Homelab Master Node Environment Configuration
# ==============================================================================

# Node Identification
NODE_NAME=zap-vps
TS_HOSTNAME=zap-vps

# Tailscale Authentication (Optional: Pre-authenticate container)
# Generate reusable or ephemeral auth key from https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=
TS_EXTRA_ARGS=--reset

# Persistent Storage Root
DATA_DIR=/srv/data

# Arcane Cockpit Manager
ARCANE_PORT=3552
ARCANE_APP_URL=http://localhost:3552
ALLOW_CLI_PASSWORD_RESET=true
PUID=1000
PGID=1000

# Maintenance Schedule (for host auto-update timer)
UPDATE_DAY=Sun
UPDATE_TIME=04:00
